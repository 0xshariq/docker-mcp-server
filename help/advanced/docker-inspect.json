{
  "name": "docker-inspect",
  "aliases": ["dinspect"],
  "category": "Advanced System Operations",
  "description": "Display detailed information about Docker objects (containers, images, networks, volumes)",
  "usage": "docker-inspect <objectType> <objectId> [options]",
  "examples": [
    {
      "command": "dinspect container mycontainer",
      "description": "Inspect a container's configuration and state"
    },
    {
      "command": "dinspect image nginx:latest",
      "description": "Inspect an image's layers and metadata"
    },
    {
      "command": "dinspect network bridge",
      "description": "Inspect network configuration and connected containers"
    },
    {
      "command": "dinspect volume myvolume",
      "description": "Inspect volume details and mount information"
    },
    {
      "command": "dinspect --help",
      "description": "Show this help message"
    }
  ],
  "object_types": [
    "container - Inspect running or stopped containers",
    "image - Inspect image layers, configuration, and history",
    "network - Inspect network settings and connected containers",
    "volume - Inspect volume configuration and usage"
  ],
  "options": [
    {
      "flag": "--format <format>",
      "description": "Format output using Go template syntax"
    },
    {
      "flag": "--size",
      "description": "Display total file sizes (containers only)"
    },
    {
      "flag": "--type <type>",
      "description": "Return JSON for specified type"
    },
    {
      "flag": "-h, --help",
      "description": "Show this help message"
    }
  ],
  "format_examples": [
    "{{.NetworkSettings.IPAddress}} - Get container IP",
    "{{.Config.Image}} - Get image name",
    "{{.State.Running}} - Check if container is running",
    "{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}} - All IPs"
  ],
  "notes": [
    "💡 Returns detailed JSON information about Docker objects",
    "🔧 Use --format to extract specific fields from JSON output",
    "📊 Essential for debugging container and network issues",
    "⚡ Can inspect multiple objects by providing multiple names/IDs"
  ]
}
