{
  "name": "docker-volume",
  "aliases": ["dvolume"],
  "category": "Advanced Volume Management",
  "description": "Manage Docker volumes for persistent data storage",
  "usage": "docker-volume <action> [volumeName] [options]",
  "examples": [
    {
      "command": "dvolume list",
      "description": "List all Docker volumes"
    },
    {
      "command": "dvolume create myvolume",
      "description": "Create a new volume"
    },
    {
      "command": "dvolume inspect myvolume",
      "description": "Display detailed information about a volume"
    },
    {
      "command": "dvolume remove myvolume",
      "description": "Remove a volume"
    },
    {
      "command": "dvolume prune",
      "description": "Remove all unused volumes"
    },
    {
      "command": "dvolume --help",
      "description": "Show this help message"
    }
  ],
  "actions": [
    "list - List all volumes",
    "create - Create a new volume",
    "remove - Remove one or more volumes",
    "inspect - Display detailed information on one or more volumes",
    "prune - Remove all unused local volumes"
  ],
  "options": [
    {
      "flag": "--driver <driver>",
      "description": "Specify volume driver name (default: local)"
    },
    {
      "flag": "--label <label>",
      "description": "Set metadata on a volume"
    },
    {
      "flag": "--opt <option>",
      "description": "Set driver specific options"
    },
    {
      "flag": "-f, --force",
      "description": "Force the removal of one or more volumes"
    },
    {
      "flag": "-h, --help",
      "description": "Show this help message"
    }
  ],
  "volume_drivers": [
    "local - Default driver, stores data on Docker host",
    "nfs - Network File System for shared storage",
    "s3fs - Amazon S3 storage driver",
    "azure - Azure File Storage driver",
    "gce - Google Cloud Storage driver"
  ],
  "notes": [
    "💡 Volumes persist data beyond container lifecycle",
    "🔧 Volumes are preferred way to persist data in Docker",
    "📊 Volumes can be shared between multiple containers",
    "⚡ Volumes are managed by Docker and stored outside container filesystem"
  ]
}
