{
  "name": "docker-prune",
  "aliases": ["dprune"],
  "category": "Advanced System Operations",
  "description": "Remove unused Docker objects to free up disk space",
  "usage": "docker-prune [objectType] [options]",
  "examples": [
    {
      "command": "dprune",
      "description": "System-wide cleanup of all unused objects"
    },
    {
      "command": "dprune images",
      "description": "Remove unused images only"
    },
    {
      "command": "dprune containers",
      "description": "Remove stopped containers only"
    },
    {
      "command": "dprune networks",
      "description": "Remove unused networks only"
    },
    {
      "command": "dprune volumes",
      "description": "Remove unused volumes only"
    },
    {
      "command": "dprune --help",
      "description": "Show this help message"
    }
  ],
  "object_types": [
    "images - Remove dangling and unused images",
    "containers - Remove stopped containers",
    "networks - Remove networks not used by containers",
    "volumes - Remove volumes not used by containers",
    "system - Remove all unused objects (default)"
  ],
  "options": [
    {
      "flag": "-f, --force",
      "description": "Do not prompt for confirmation"
    },
    {
      "flag": "--all",
      "description": "Remove all unused images, not just dangling ones"
    },
    {
      "flag": "--filter <filter>",
      "description": "Provide filter values (e.g., 'until=24h')"
    },
    {
      "flag": "--volumes",
      "description": "Prune volumes as well (system prune only)"
    },
    {
      "flag": "-h, --help",
      "description": "Show this help message"
    }
  ],
  "filter_examples": [
    "until=24h - Objects older than 24 hours",
    "until=2021-01-01 - Objects older than specific date",
    "label=maintainer=john - Objects with specific label",
    "label!=production - Objects without production label"
  ],
  "safety_warnings": [
    "⚠️  CAUTION: Prune operations are irreversible",
    "⚠️  Always backup important data before pruning volumes",
    "⚠️  Use --force only in automated scripts",
    "⚠️  Review what will be deleted before confirming"
  ],
  "notes": [
    "💡 Regular pruning helps maintain clean Docker environment",
    "🔧 System prune is safest - doesn't remove volumes by default",
    "📊 Shows space reclaimed after operation completes",
    "⚡ Use filters to prune objects based on age or labels"
  ]
}
